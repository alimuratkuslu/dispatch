import Foundation

/// INSTRUCTIONS:
/// 1. Copy this file and rename it to 'Secrets.swift'
/// 2. Replace 'YOUR_CLIENT_ID' with your actual GitHub OAuth Client ID.
/// 3. Make sure 'Secrets.swift' is NOT tracked by Git (verified in .gitignore).

enum Secrets {
    static let githubClientID = "YOUR_CLIENT_ID"
}
